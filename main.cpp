#include <cstdio>

#include "hft/types.hpp"

// Entry point. Intentionally empty — wire your engine in here once the modules
// exist. Right now it just proves the build, includes, and link work.
int main() {
    std::printf("hft_core skeleton: build OK.\n");
    return 0;
}
